/*create database locadora;
use locadora;

create table carro(
    modelo VARCHAR(50),
    cor VARCHAR(50),
    ano INTEGER,
    potencia FLOAT,
    marca VARCHAR(50),
    preco FLOAT
);

create table cliente(
    cpf INTEGER,
    nome VARCHAR(50)
)
;
create table funcionario(
    nome VARCHAR(50),
    idade INTEGER,
    funcao VARCHAR(50)
)*/

select * from cliente;